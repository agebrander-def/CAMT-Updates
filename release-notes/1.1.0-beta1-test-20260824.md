# CAMT 1.1.0 Beta 1 — Update Manager detection test

**Build:** `110B1-RCF-20260824`  
**Date:** 2026-08-24  
**Channel:** Beta

This is a **test manifest only**.

Purpose:
- verify that CAMT Update Manager detects a newer build;
- verify build-ID comparison across laptops;
- verify that `Update available` is shown.

No installer is published for this test, so `download_url` and `sha256` are intentionally empty.
