# CAMT Updates

Update repository for CAMT Professional Edition.

The CAMT Update Manager reads `update.json` from the `main` branch. Publish installers as GitHub Release assets, then update `download_url` and `sha256`. Never commit the private signing key.
